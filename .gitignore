.quarto/

/.quarto/
**/*.quarto_ipynb
con_trees.*.txt
mod_trees.*.txt
bcf_log_*.txt

# knitr chunk caches (local only; NOT needed for deploy — _freeze/ is)
*_cache/

# fetched data cache (see estimation.qmd); vendoring it instead would be stronger
cattaneo2.dta
